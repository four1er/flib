[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "tinysglang"
version = "0.1.0"
description = "A tiny sglang implementation"
requires-python = ">=3.10"
dependencies = [
    "torch",
]

[tool.setuptools.packages.find]
where = ["."]
